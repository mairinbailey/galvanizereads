<div class="row">
  <div class="col-md-offset-2 col-md-6 col-xs-12">
    <img class="book-cover" src="{{book.cover_url}}" alt="cover_url"/>
    <div class="col-md-4">
      <h3>{{book.title}}</h3>
      <p>{{book.genre}}</p>
      <h5>{{book.description}}</h5>
      <a class="btn btn-default" href=".{{this.id}}/confirmDelete">Delete</a>

  </div>
</div>
